-- Example SQL queries for the users table

-- Select all users
SELECT * FROM users;

-- Insert a new user
-- INSERT INTO users (name) VALUES ('Charlie');

-- Select users with ID greater than 1
SELECT id, name FROM users WHERE id > 1; 